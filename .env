# Base URL of your Express backend. Should NOT include a trailing slash.
# Example: https://taskforge-api.up.railway.app/api
VITE_API_URL=http://localhost:5001/api

# Set to "true" to use localStorage mock instead of real backend
# Use "false" to connect to the MongoDB backend
VITE_USE_MOCK=false
